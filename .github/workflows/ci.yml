name: CI/CD Pipeline - Frontend

on:
  push:
    branches: [ "develop", "main", "Develop" ]
  pull_request:
    branches: [ "develop", "main", "Develop" ]

jobs:
  # INTEGRACIÓN CONTINUA (CI)
  build-and-validate:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar código
        uses: actions/checkout@v4

      - name: Validar archivos obligatorios
        run: |
          test -f Dockerfile || (echo "ERROR: Falta Dockerfile" && exit 1)
          test -f .env.example || (echo "ERROR: Falta .env.example" && exit 1)
          test -f README.md || (echo "ERROR: Falta README.md" && exit 1)

      - name: Validar compilacion de Dockerfile Frontend
        run: docker build -t frontend-app-test .

  # DESPLIEGUE CONTINUO: DESARROLLO (Ubuntu Server / Tailscale)
  deploy-dev:
    name: Despliegue a Entorno de Desarrollo
    needs: build-and-validate
    if: github.event_name == 'push' && (github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/Develop')
    runs-on: ubuntu-latest

    steps:
      - name: Descargar código
        uses: actions/checkout@v4

      - name: Conectar a la VPN de Tailscale
        uses: tailscale/github-action@v3
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci

      - name: Desplegar en Ubuntu Server vía SSH sobre Tailscale
        env:
        
          SSH_KEY: ${{ secrets.DEV_SERVER_SSH_KEY }}
          DEV_HOST: ${{ secrets.DEV_SERVER_HOST }}
          DEV_USER: ${{ secrets.DEV_SERVER_USER }}
        run: |
          # 1. Configurar la llave SSH de forma segura en el runner
          mkdir -p ~/.ssh
          echo "$SSH_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          
          # 2. Configurar SSH con el túnel de Tailscale
          cat <<EOF > ~/.ssh/config
          Host dev-server
              HostName $DEV_HOST
              User $DEV_USER
              IdentityFile ~/.ssh/id_ed25519
              StrictHostKeyChecking no
              ProxyCommand tailscale nc %h %p
          EOF

          # 3. Ejecutar actualización del servidor y despliegue del contenedor
          ssh dev-server << 'EOF'
            cd /home/Brxckin/Frontend-Arquitectura1
            git pull origin Develop
            docker build -t frontend-dev .
            docker container inspect frontend-dev-container >/dev/null 2>&1 && docker stop frontend-dev-container && docker rm frontend-dev-container || true
            docker run -d --name frontend-dev-container --env-file .env -p 3000:3000 frontend-dev
          EOF
